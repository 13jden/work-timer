# TASK-040 · 多账户钱包 + 存钱目标（Phase 5）

| 字段 | 值 |
|---|---|
| **分支** | 待定（可与 TASK-039 并行） |
| **所属版本** | v2.4 |
| **依赖** | TASK-037（MINE 占位页）；与 TASK-039 相对独立 |
| **优先级** | P1 |
| **状态** | ⏳ 待细化 |

---

## 1. 背景

记账记录已关联 `accountId`，但账户管理（多钱包）与存钱目标尚未实现。本任务补齐记账主题 MINE 页（tab 3）的钱包与目标区域。

## 2. 任务清单

| ID | 任务 | 说明 |
|---|---|---|
| T-401 | 多账户管理 | 账户增删改（名称/类型/余额/颜色），删除前校验关联记录 |
| T-402 | 总资产卡片 | MINE 页顶部总资产 = 各账户余额合计 |
| T-403 | 记录关联账户 | 完整添加弹窗账户选择（快速记录沿用默认账户） |
| T-404 | 存钱目标 | 创建/进度/关联账户，进度条 + 预计完成时间 |
| T-405 | MINE 页集成 | 替换最后一个占位页：钱包 + 池（039）+ 存钱目标 |
| T-406 | 池收入/支出方向 | `PoolConfig.direction`：收入池/支出池，生成记录与认领按方向记账，建池弹窗方向开关（分类按方向过滤） |
| T-407 | 账户颜色小点 | 账户弹窗自定义卡片小点颜色（8 色预设），资产卡片按 `account.color` 渲染 |
| T-408 | 拖拽归入账户 | 统计页日视图 dnd-kit：整行拖动 → 底部账户 dock → 松手归入；行内账户标签（未归入红色虚线标记），余额随 `updateRecord` 自动迁移 |
| T-409 | 虚拟资产分解 | 总资产卡 = 实际 + 预付未消耗（淡红）+ 已赚未到账（淡绿）；未归入记录与先用后付「未支付」同虚线框展示（如 `-¥100 未分类  -¥280 未支付`，房租 2100 日均 70、4 号欠 280 场景）；统计页池虚拟记录标记「虚拟到账/虚拟均摊」；收入池卡片显示到账进度与未到账金额；记录弹窗支持「未归入」账户。纯函数 `calcVirtualAssets`（virtual.ts）+ 13 条单测 |
| T-410 | 池自动退休 + 记录溯源 + 记录编辑 | ① 快速记录不绑账户（`accountId=''`）→ 进总资产「未分类」调整项；② 均摊记录带周期快照（`poolCycleStart/End/Total`）与池名快照（`poolName`），认领满额回写实际关联时间与金额（`poolSettledAt/Amount`，虚拟变实际）；③ `retireFinishedPools`：满额且周期已过的均摊池自动移除，只删池与周期元数据，记录全部保留；④ 统计页日视图用 `listableRecords` 展示认领付款记录（「已关联池 · 不计统计」标记），汇总仍走 `visibleRecords` 不重复计算；⑤ 记录行点击打开编辑弹窗（改金额/分类，保存清除未分类状态），拖拽后抑制误触点击；⑥ 编辑弹窗展示均摊池只读信息（周期/总额/实际关联），`recordAffectsBalance` 判定余额迁移（均摊记录编辑/删除不动余额）。+9 条单测（370 全绿） |

> T-406 ~ T-410 为 v2.4 用户验收过程中的增量需求（2026-09-04），状态：代码完成，待验收。

## 3. 关键设计点（开发前细化）

- **余额更新策略**：记账时自动增减账户余额，还是仅展示手动维护的余额？（开发前与用户对齐）
- **存钱目标入账**：转入目标账户的记录自动累计 `currentAmount`，还是手动更新？
- `SavingsGoal` 数据模型已在 v2.0 数据层落地（见 ROADMAP 存档）

## 4. 验收标准

- [ ] 多账户增删改数据正确，删除账户时关联记录有兜底处理
- [ ] 总资产卡片实时汇总
- [ ] 添加记录可选账户，账户余额联动正确
- [ ] 存钱目标进度实时更新
- [ ] MINE 页（记账主题 tab 3）完整可用，占位页全部替换完毕
- [ ] typecheck / test / build 全绿 + 用户验收通过

## 5. 不做的事

- ❌ 账户间转账（可在 TASK-041 评估）
- ❌ 多币种

---

*创建于 2026-09-03 · 状态：待细化*
