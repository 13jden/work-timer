# 复制成服务器项目目录下的 .env(docker compose 会自动读取;.env 已被 git 忽略)
#
# 默认不需要任何配置:站点就是 persecond.work,证书由 Caddy 走 Let's Encrypt 自动签发 + 自动续签。
# 这个文件只在「要换域名」时才需要存在。

# 站点域名(裸域名,不要带 https:// 和端口)
SITE_DOMAIN=persecond.work

# 换子域名的写法:
# SITE_DOMAIN=app.persecond.work
# 一张证书包含多个域名(逗号分隔,前提是每个域名都有解析记录):
# SITE_DOMAIN=persecond.work, www.persecond.work

# ── 前置条件(重要) ──
# 1. 域名 A 记录指向服务器公网 IP(已确认 persecond.work -> 47.104.228.220)
# 2. 阿里云安全组放行 443:证书用 TLS-ALPN-01 在 443 上完成验证,浏览器访问也走它
# 3. 80 端口目前被阿里云建站产品自带的 OpenResty 占着,由它把 http 301 到 https,不要去抢;
#    哪天退订了那个建站产品,再把 docker-compose.yml 的 ports 加回 "80:80",让 Caddy 自己做跳转
# 4. 国内服务器用域名对外提供服务需 ICP 备案(已备案)
#
# 改完之后重跑 GitHub Actions 的 Deploy 工作流即可,或在服务器上:
#   docker compose up -d --force-recreate
