# 复制成服务器项目目录下的 .env(docker compose 会自动读取,.env 已被 git 忽略)
#
# ── 模式一:有域名(推荐,Let's Encrypt 免费证书 + 自动续签 + 80→443 跳转) ──
# 前置条件:
#   1. 域名 A 记录已解析到服务器公网 IP(生效后再部署,否则 ACME 验证失败)
#   2. 云安全组 / 防火墙放行 80 和 443(80 用于 HTTP-01 验证,不能只开 443)
#   3. 域名在国内服务器上访问需已完成 ICP 备案,否则可能被运营商阻断
#
SITE_DOMAIN=salary.example.com
ACME_EMAIL=you@example.com

# ── 模式二:只有公网 IP / 内网临时验证 ──
# 把上面两行注释掉或留空即可,compose 会回落到默认值:
#   SITE_ADDRESS=:443  TLS_ARG=internal
# Caddy 用内置 CA 自签证书,浏览器会提示不安全需手动信任;
# 证书不受信任时 Service Worker 注册与 PWA 安装不保证可用,正式使用请配域名。
#
# SITE_DOMAIN=
# ACME_EMAIL=

# ── 改完之后 ──
# 服务器上执行: docker compose up -d --force-recreate
# 或直接重跑 GitHub Actions 的 Deploy 工作流
