/**
 * SlackingDetailPage — 兼容 alias
 *
 * v1.3.3:文件重命名为 `TimeTrackerDetailPage.tsx`,
 * 旧 import 路径 `from './SlackingDetailPage'` 仍可用(透传 re-export)。
 *
 * 推荐新代码:`from './TimeTrackerDetailPage'`。
 */
export { TimeTrackerDetailPage as SlackingDetailPage } from './TimeTrackerDetailPage';
